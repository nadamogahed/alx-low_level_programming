#include "main.h"
/**
 * _abs - prints the alphabet
 * @i: single letter input.
 * Return: 1 if c is lowercase
*/
int _abs(int n)
{
	if (n < 0)
	{
		n = n / -1;
		return (n);
	}
	else
	{
		return (n);
	}
	return (0);
}
