#include "main.h"
/**
 * _islower - prints the alphabet
 * Description: prints the alphabet in lowercase.
 * Return: 1 if c is lowercase
*/
int _islower(int)
{
	int c;

	if (islower (c) == True)
	{
		return (1);
	}
	_putchar('\n');
	return (0);
}
