#include "main.h"
/**
 * _isalpha - prints the alphabet
 * @c: single letter input.
 * Return: 1 if c is lowercase
*/
int _abs(int i)
{
	if (i < 0)
	{
		i = i / -1;
		return (i);
	}
	else
	{
		return (i);
	}
}
