#include "main.h"
/**
 * add - takes two integers
 * @i: first input
 * @x: second input
 * Description: prints addtion of two numbers.
 * Return: retrurn the sum value
*/
int add(int i, int x)
{
	return (i + x);
}
