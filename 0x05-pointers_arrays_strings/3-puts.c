#include "main.h"
/**
 * _isdigit - check if its digit or char
 * @c: input.
 * Return: 1 if c is uppercase
*/
void _puts(char *str)
{
	while (*str != '\0')
	{
		_putchar(*str);
	}
	_putchar('\n');
}
